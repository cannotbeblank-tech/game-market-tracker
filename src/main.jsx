import React from 'react';
import ReactDOM from 'react-dom/client';
import GameMarketTracker from './GameMarketTracker';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GameMarketTracker />
  </React.StrictMode>
);